  
<view
  class="custom-class ss-font ss-icon-{{ name }} "
  style="color:{{color?'color:'+color:''}};font-size:{{size?'font-size:'+size:''}};display:{{block?'display:block':''}}"
  bind:tap="onClick"
>
  <view wx:if="{{ info !== null }}" class="ss-icon__info">{{ info }}</view>
</view>